'use strict';
export default function weekDayFunction(){
        let days = ["Вc", "Пн", "Вт", "Ср", "Чт", "Пт", "Сб"];
        let weekDay = days[new Date().getDay()];
        return weekDay;
}
export default weekDayNumber = [new Date().getDay()]


